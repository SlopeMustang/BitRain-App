# 사용법

BitRain은 **DAC에 그대로 내보내는 것**을 목적으로 만든 플레이어입니다. 설정 대부분이 그 한 가지를 위해 있습니다.

- [처음 한 번](#처음-한-번)
- [오디오 출력 — 가장 중요한 설정](#오디오-출력--가장-중요한-설정)
- [재생](#재생)
- [음질 도구](#음질-도구)
- [라이브러리](#라이브러리)
- [NAS에서 쓸 때](#nas에서-쓸-때)
- [문제가 생기면](#문제가-생기면)

---

## 처음 한 번

**설정(⌘,) › Library › Watched Folders**에서 음악이 있는 폴더를 추가합니다. 하위 폴더까지 훑어 라이브러리를 만듭니다.

첫 스캔은 곡 수와 저장 위치에 따라 시간이 걸립니다. **NAS에 수천 곡이 있으면 특히 오래 걸리며, 한 번 끝나면 이후에는 바뀐 것만 확인합니다.**

## 오디오 출력 — 가장 중요한 설정

**설정 › Audio Output** (⌥⌘O)

### Output Device

소리를 내보낼 장치를 고릅니다. USB DAC을 연결했다면 여기서 선택해야 그쪽으로 나갑니다. 아래 **Device Info**에 그 장치가 실제로 지원하는 샘플레이트 목록이 나오므로, 내 DAC이 어디까지 받는지 여기서 확인할 수 있습니다.

### Exclusive Mode

| 항목 | 하는 일 |
|---|---|
| **Hog Mode (exclusive device access)** | 재생하는 동안 DAC을 **독점**합니다. 다른 앱의 알림음이나 브라우저 소리가 섞이지 않습니다 |
| **Automatic sample rate switching** | 트랙마다 **원본 샘플레이트로 DAC을 맞춥니다.** 44.1kHz 곡은 44.1kHz로, 96kHz 곡은 96kHz로 |

**두 개를 켜는 것이 이 앱을 쓰는 이유에 가깝습니다.**

자동 전환을 끄면 장치 레이트를 건드리지 않습니다. 그런데 곡은 원본 레이트로 디코드되므로, **장치가 다른 레이트에 있으면 그 사이를 시스템이 변환합니다.** 48kHz에 머문 장치로 44.1kHz 곡을 재생하면 44.1 → 48 변환이 신호 경로에 들어갑니다 — 비트퍼펙트가 깨지는 지점입니다.

> Hog Mode를 켜면 재생 중에는 다른 앱에서 소리가 나지 않습니다. 정상 동작입니다.

### 창 위쪽에서 바로 보고 바꾸기

이 설정들은 **창 왼쪽 위에 항상 떠 있습니다.** 설정을 열지 않아도 지금 어떤 상태인지 보이고, 눌러서 바꿀 수도 있습니다.

```
[⌇ Off ▾]  [🔊 DX3 Pro+ ▾]  [🔒]  [⇄]
 업샘플링       출력 장치      독점  자동 전환
```

**켜져 있으면 빨간색**입니다. 각 항목에 커서를 잠시 올리면 현재 상태에 맞는 설명이 뜹니다.

독점 모드는 **자물쇠 모양이 세 가지**입니다.

| 모양 | 뜻 |
|---|---|
| 열린 자물쇠 · 회색 | 꺼짐 |
| 닫힌 자물쇠 · 빨강 | 켜 뒀지만 **아직 장치를 잡지 않음** |
| 채워진 자물쇠 · 빨강 | **지금 장치를 독점 중** |

가운데와 마지막을 구분하는 이유가 있습니다. **독점은 재생을 시작할 때 잡습니다.** 그래서 켜 두기만 하고 재생하지 않으면 잡히지 않고, 다른 앱이 이미 장치를 쥐고 있으면 잡는 데 실패합니다. 「켰는데 왜 다른 소리가 섞이지」 싶을 때 여기를 보면 됩니다.

### Upsampling

원본보다 높은 레이트로 올려 내보냅니다. **Off**가 기본이며, 이것이 원본 그대로입니다.

- **2x · 4x · 8x · 16x** — 배수로 올립니다
- **Max** — DAC이 지원하는 가장 높은 레이트까지 올립니다

**44.1kHz 계열은 88.2·176.4·352.8kHz로, 48kHz 계열은 96·192·384kHz로 올라갑니다.** 두 계열을 섞지 않는 것은 정수배가 아닌 변환을 피하기 위해서입니다.

> 업샘플링이 항상 낫다고 보기는 어렵습니다. 원본 그대로(Off)와 비교해 보시고 정하십시오.

**업샘플링을 켜면 자동 전환이 꺼져 있어도 장치 레이트가 바뀝니다.** 목표 레이트를 정하는 기능이라 장치를 안 바꾸면 성립하지 않기 때문입니다.

## 재생

### 단축키

| 키 | 동작 |
|---|---|
| `Space` | 재생/일시정지 |
| `⌘→` `⌘←` | 다음 곡 / 이전 곡 |
| `⇧⌘→` `⇧⌘←` | 앞으로 10초 / 뒤로 10초 |
| `⌘↑` `⌘↓` | 볼륨 |
| `⌘S` | 셔플 |
| `⌘R` | 반복 모드 |
| `⇧⌘F` | 즐겨찾기 |
| `⌘F` | 라이브러리 검색 |
| `⌥⌘O` | 오디오 출력 |
| `⌥⌘E` | 파라메트릭 EQ |

### 플레이 바

- **Stop After Current Track** — 지금 곡이 끝나면 정지합니다. 다음 곡으로 넘어가지 않고 멈추므로 자기 전에 유용합니다. 곡이 끝나면 자동으로 해제되고, 직접 다음/이전 곡을 누르면 그때도 해제됩니다
- **볼륨 / 밸런스** — 오른쪽에 위아래로 있습니다. 밸런스는 좌우 ±6dB이며 **수치를 더블클릭하면 중앙으로 돌아갑니다**

> 밸런스는 반대쪽 채널을 **줄이는 방식**입니다. 어느 쪽도 키우지 않으므로 클리핑이 생기지 않고, 0(중앙)일 때는 계산 자체를 건너뛰어 **원본과 비트 단위로 같은 신호**가 나갑니다.

## 음질 도구

### 파라메트릭 EQ (⌥⌘E)

8밴드 EQ입니다. 주파수 응답 커브를 직접 끌어 조절하고 프리셋으로 저장할 수 있습니다.

### 아날로그 잡음 체크

**재생 메뉴 › 아날로그 잡음 체크**

음악을 재생하지 않은 채 **출력 스트림만 열어 디지털 무음을 내보냅니다.** 이때 들리는 소리는 전부 아날로그 쪽에서 온 것입니다 — 케이블, 전원, USB 허브, 앰프.

스피커에서 지지직거리는 소리가 나는데 원인을 모르겠다면 이것을 켜 두고 케이블을 하나씩 바꿔 보십시오. **실제로 이 기능으로 USB 허브 노이즈를 찾아낸 적이 있습니다.**

## 라이브러리

- **⌘F** — 검색
- **Folders 탭** — 파일시스템 그대로 탐색합니다. 설정 › General에서 켜고 끕니다. **⌘↑**로 상위 폴더
- **재생목록** — 만들고, 가져오고(M3U), 내보낼 수 있습니다
- **즐겨찾기** — ⇧⌘F

메타데이터가 빠진 곡이 있다면 **설정 › Library**에서 라이브러리를 새로 고칩니다.

## NAS에서 쓸 때

SMB/NFS 네트워크 스토리지를 전제로 만들어졌습니다.

- **다음 곡을 미리 읽어 둡니다.** 곡이 바뀔 때 네트워크 지연으로 끊기지 않도록
- **큰 파일은 메모리에 올리지 않고 매핑해 읽습니다.** 수 GB짜리 트랙에서도 끊김이 생기지 않습니다
- **볼륨이 마운트되지 않은 상태에서 앱을 켜면** 자동으로 마운트를 시도합니다

> **파일 이름에 한글이 있고 NAS가 Linux 계열이면** 일부 곡이 목록에 보이면서도 재생되지 않을 수 있습니다. 유니코드 정규화 방식이 서버와 macOS 사이에서 어긋나는 경우입니다. BitRain은 스캔할 때 이런 파일을 찾아 알려줍니다 — 해당 파일을 NAS에서 이름만 다시 저장하면 해결됩니다.

## 문제가 생기면

**도움말 › BitRain 도움말 › 진단**

| 버튼 | 용도 |
|---|---|
| **진단 스냅샷 복사** | 앱 설정 · 오디오 장치 · 라이브러리 통계 · 하드웨어 사양을 클립보드에 담습니다 |
| **스냅샷 보기** | 보내기 전에 내용을 직접 확인합니다 |
| **문제 신고** | 이슈 작성 화면을 엽니다 |
| **로그 파일 보기** | 상세 기록이 있는 위치를 엽니다 |

[이슈를 열어](../../issues/new/choose) 스냅샷과 함께 알려 주시면 원인을 훨씬 빨리 좁힐 수 있습니다.

**스냅샷에는 음악 파일의 내용도, 곡 제목도 들어가지 않습니다.** 재생 중인 곡에 대해서는 포맷과 샘플레이트만 담깁니다. 자동으로 전송되는 것은 없으며, 보내는 것은 직접 복사해 붙여넣을 때뿐입니다.
